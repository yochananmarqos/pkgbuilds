# http://aur.archlinux.org/packages/python-rarfile
pkgname=python-rarfile
_name=${pkgname#python-}
pkgver=3.2
pkgrel=1
pkgdesc="Python module for RAR archive reading"
arch=('any')
url="https://github.com/markokr/rarfile"
license=('ISC')
depends=('python')
makedepends=('python-setuptools')
optdepends=('unrar: Decompressing compressed files'
            'libarchive: Alternative to "unrar"'
            'python-crypto: Parsing encrypted headers')
source=("https://pypi.org/packages/source/${_name:0:1}/$_name/$_name-$pkgver.tar.gz")
sha256sums=('422cbf788c5fed87ed0d55aff88951f54378ec84749eb05cc0584d87f2637545')

build() {
	cd "$_name-$pkgver"
	python setup.py build
}

package() {
	cd "$_name-$pkgver"
	python setup.py install --root="$pkgdir" --optimize=1 --skip-build

	install -Dm644 LICENSE -t "$pkgdir/usr/share/licenses/$pkgname"
}
