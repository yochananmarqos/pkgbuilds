# https://aur.archlinux.org/packages/waydroid-helper

pkgname=waydroid-helper
pkgver=0.2.6
pkgrel=1
pkgdesc="GUI helper for Waydroid configuration and extensions"
arch=('any')
url="https://github.com/ayasa520/waydroid-helper"
license=('GPL-3.0-or-later')
depends=(
  'android-tools'
  'gtk4'
  'fakeroot'
  'libadwaita'
  'python'
  'python-aiofiles'
  'python-cairo'
  'python-dbus'
  'python-gobject'
  'python-httpx'
  'python-pywayland'
  'python-yaml'
  'waydroid'
)
makedepends=('meson')
optdepends=('bindfs: shared folders support')  # AUR
source=("$pkgname-$pkgver.tar.gz::$url/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('7f5168197080bb5034f218c7f0a10e0277f86544fed9debb624d49625b1efb31')

build() {
  arch-meson "$pkgname-$pkgver" build
  meson compile -C build
}

check() {
  meson test -C build --no-rebuild --print-errorlogs || :
}

package() {
  meson install -C build --no-rebuild --destdir "$pkgdir"
}
