# https://aur.archlinux.org/packages/chromecast_mpris
groups=('modified')

pkgname=chromecast_mpris
pkgver=0.8.0
pkgrel=1
pkgdesc="Control Chromecasts from Linux and D-Bus"
arch=('any')
url="https://github.com/alexdelorenzo/chromecast_mpris"
license=('AGPL3')
depends=('python-pychromecast=9.1.2' 'python-gobject>=3.34.0' 'python-pydbus>=0.6.0'
         'python-click>=7.0' 'python-mpris_server=0.2.18')
makedepends=('python-setuptools')
optdepends=('playerctl: Get the D-Bus name for your device'
            'gnome-shell-extension-mpris-indicator-button: MPRIS indicator for GNOME')
source=("https://pypi.org/packages/source/${pkgname:0:1}/$pkgname/$pkgname-$pkgver.tar.gz")
sha256sums=('9c296cfabf11577c591155c58cd3e04b348bcb2dd7dff7986468520ac9304bed')

build() {
	cd "$pkgname-$pkgver"
	python setup.py build
}

package() {
	cd "$pkgname-$pkgver"
	export PYTHONHASHSEED=0
	python setup.py install --root="$pkgdir" --optimize=1 --skip-build
}
