pkgname=planify-devel
_app_id=io.github.alainm23.planify
pkgver=3.0.7+44+g71ebb7c1
pkgrel=1
pkgdesc="Task manager with Todoist support (Development version)"
arch=('x86_64')
url="https://useplanner.com"
license=('GPL3')
depends=('evolution-data-server' 'granite7' 'gtksourceview5' 'libadwaita'
         'webkitgtk-6.0')
makedepends=('git' 'gobject-introspection' 'meson' 'vala')
checkdepends=('appstream')
provides=('planify')
conflicts=('planify' 'elementary-planner' 'task-planner')
replaces=('elementary-planner' 'task-planner-devel')
_commit=71ebb7c14f9d4aa973fa5556742ee9a4b3e75e6c  # branch/master
source=("git+https://github.com/alainm23/planner.git#commit=$_commit")
sha256sums=('SKIP')

pkgver() {
  cd planner
  git describe --tags | sed 's/-/+/g'
}

build() {
  arch-meson planner build
  meson compile -C build
}

check() {
  desktop-file-validate "build/data/${_app_id}.Devel.desktop"
  appstreamcli validate --no-net "build/data/${_app_id}.Devel.appdata.xml" || :
}

package() {
  meson install -C build --destdir "$pkgdir"

  ln -s "/usr/bin/${_app_id}" "$pkgdir/usr/bin/planify"
}
